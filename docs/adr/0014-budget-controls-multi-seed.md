# Budget Profile controls multi-seed execution

The system keeps multi-seed evaluation as a Core Research Loop capability, but the number of seeds is controlled by the selected Budget Profile. Low-budget runs may use one seed and must treat results as preliminary; higher-budget runs may use multiple seeds and may claim stronger stability evidence.
