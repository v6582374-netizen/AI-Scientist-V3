# Start with general and machine_learning Domain Packs

The first generalized version will support only two Domain Packs: `general` and `machine_learning`. `general` is domain-neutral and must avoid field-specific assumptions; `machine_learning` preserves the current ML-oriented behavior, vocabulary, and experimental guidance without allowing those assumptions to remain in the Core Research Loop.
