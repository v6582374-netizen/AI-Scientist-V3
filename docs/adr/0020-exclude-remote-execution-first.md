# Exclude remote execution from the first version

The first generalized implementation assumes ideation and experiment execution happen on the same local project environment. Remote or cloud GPU execution is out of scope for the first version, so Execution Backends cover local modes only.
