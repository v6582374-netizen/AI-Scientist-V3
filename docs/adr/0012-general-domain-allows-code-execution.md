# Allow code execution in the general Domain Pack

Executable code remains a Core Capability and is not specific to machine learning. The `general` Domain Pack may still generate and execute code for simulation, statistics, numerical analysis, data processing, or other domain-neutral validation, but it must not inject ML-specific training, CUDA, dataset, or benchmark assumptions.
