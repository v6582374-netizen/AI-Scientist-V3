# Require explicit research input

The generalized flow must not use bundled ML examples as default research input. Users must explicitly provide a research topic, workshop file, or pregenerated idea file so automatic domain selection is based on the user's intent rather than repository examples.
