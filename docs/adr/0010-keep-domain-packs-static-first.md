# Keep first-version Domain Packs static

First-version Domain Packs are static built-in configuration objects that provide prompt guidance and default preferences for execution backend and Budget Profile. They must not register arbitrary tools, replace the Core Research Loop, modify tree search behavior, or dynamically load third-party code; those plugin capabilities can be considered after the generalization architecture is proven.
