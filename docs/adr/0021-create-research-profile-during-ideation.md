# Create Research Profile during ideation

Research Profile is created during idea generation and saved with the generated ideas. Experiment execution reads that profile by default and does not automatically re-plan for a different environment; only explicit user overrides may change domain, execution backend, or Budget Profile during the run.
