# Keep ideation and experiment run separate first

The first generalized implementation should preserve the current two-step workflow: idea generation produces a pregenerated ideas file, and `launch_scientist_bfts.py` runs experiments and writeup from that file. Both stages should use the Research Profile language, but a one-command end-to-end workflow is a future extension.
