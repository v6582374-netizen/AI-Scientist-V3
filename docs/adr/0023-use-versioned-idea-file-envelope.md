# Use a versioned idea file envelope

Generated idea files should use a top-level envelope with `schema_version`, `research_profile`, and `ideas`. The Research Profile belongs to the ideation run rather than being duplicated inside each idea, and schema versioning keeps startup validation simple.
